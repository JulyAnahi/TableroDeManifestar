<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Profile</title>
    <%- include("partials/head") %>
</head>
<body>
    <%- include('partials/navigation') %>

      
    <div class="row g-3">
        
        <div class="d-flex justify-content-center col-12 col-lg-4 mx-auto">
            <div class="card animate__animated animate__fadeInDown h-100">
              <img src="<%= image.path %>" alt="" class="card-img-top">
              <div class="card-body">
                 <h5 class="card-title"><%= image.title %></h5>
                 <p class = "card-text"><%= image.description %></p>
                 <p class = "card-text"><%= format(image.created_at)%></p>
                 <a href="/image/<%= image._id %>/delete" class="btn btn-danger d-grid gap-2 p-2">
                    Eliminar imagen
                 </a>
                 
              </div>
            </div>
          </div>
      </div>
      
    
</body>
</html>